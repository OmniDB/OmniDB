# Tests with PostgreSQL

In order to execute automated tests with PostgreSQL, you need:

- Install PostgreSQL somewhere
- Change database connection parameters in tests.conf
- Restore OmniDB_app/tests/database/dellstore2-normal-1.0.sql inside an empty
  database
    - psql -d omnidb_tests -U omnidb -f dellstore2-normal-1.0.sql


# Tests with Selenium (automating use of browsers)

- Put file OmniDB_app/tests/webdrivers/geckodriver in your PATH
